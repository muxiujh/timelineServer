@using JCore;
@model Object
@{
    TFieldLogic fields = new TFieldLogic();
    fields.Row2UI(Model, dict => {
        if (dict != null) {
            Html.RenderPartial(dict[TF.widget].ToString(), dict);
        }
    });
}
