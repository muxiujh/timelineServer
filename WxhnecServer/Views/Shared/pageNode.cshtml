@{ Layout = null; }

@using JCore;
@model SPageNode
@{
    string active = Model.IsActive ? "active" : "";
    Controller C = ViewBag.controller;
}
<li class='@active'><a href='@C.U("p", Model.Number)'>@Model.Text</a></li>