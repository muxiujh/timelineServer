@{
    Layout = "~/Views/Admin/_RowLayout.cshtml";
}

@using JCore;
@model Object
@{
    TEntityUI entityUI = new TEntityUI((TS)Session[G.super]);
    entityUI.Row2UI(Model, dict => {
        Html.RenderPartial("Row/" + dict[TF.widget].ToString(), dict);
    });
}
